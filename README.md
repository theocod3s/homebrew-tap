# homebrew-tap

Homebrew tap for [theocod3s](https://github.com/theocod3s) tools.

```sh
brew install theocod3s/tap/<name>
```

`brew tap theocod3s/tap` resolves to this repository — the `homebrew-` prefix is a naming
convention, not part of the tap's name.
